import React from 'react';
import Styled from './styles';

const Navbar = () => (
  <Styled.Navbar>
    <div>logo</div>
    <Styled.Menu>
      <div>menu</div>
      <div>Download CV</div>
    </Styled.Menu>
  </Styled.Navbar>
);

export default Navbar;
